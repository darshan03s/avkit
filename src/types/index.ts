export type VideoCropRect = { left: number; top: number; width: number; height: number }

export type DisplayCropRect = { x: number; y: number; w: number; h: number }

export type DragHandle = 'nw' | 'n' | 'ne' | 'e' | 'se' | 's' | 'sw' | 'w' | 'move'
